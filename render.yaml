services:
  - type: web
    name: smartharvest-web
    env: python
    plan: free
    buildCommand: "./build.sh"
    startCommand: "gunicorn smart_harvest.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4
      - key: DEBUG
        value: "False"
